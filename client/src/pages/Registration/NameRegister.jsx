import React from 'react'

function NameRegister({onClick}) {
  return (
    <div>
      <div>Name Component</div>
      <button onClick={onClick}>Next</button>
    </div>
  )
}

export default NameRegister
