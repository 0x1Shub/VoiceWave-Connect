import React from 'react'

function ProfileRegister({onClick}) {
  return (
    <div>
        <div>Profile Component</div>
        <button onClick={onClick}>Next</button>
  </div>
  )
}

export default ProfileRegister
